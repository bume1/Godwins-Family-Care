# OpenEMR coverage

_Generated by `node scripts/openemr_coverage.js` from `openemrCoverage.js`. **Do not hand-edit** — a hand-maintained list of what works is a list that goes stale silently._

What this app does with OpenEMR, in three lists, because "written" and "proven" are different facts and the gap between them is where five sessions have lost time.

## Proven against the live instance (23)

Exercised against the running EMR with the **stored value read back** — not a status code. This repo has been burned six times by a 200 that wrote nothing, so a row is only here when somebody read the data back out.

| Resource | Kind | Since | What to know |
|---|---|---|---|
| `Patient` | read | 4.1 | Search and read; the link step creates through it. Exercised in verify_43_patient_read. |
| `Condition` | read | 4.1 | Returns, but on this instance carries NO ICD coding — OpenEMR's FHIR mapper drops it (settled 2026-09-08). The standard API returns the coding, so T1 fills it back in from the app's prior coded encounter. |
| `AllergyIntolerance` | read | 4.1 | The allergen is in text.div, NOT in code — code holds the data-absent reason "Unknown", which displayed as every allergy reading "Unknown" until 2026-09-08. |
| `MedicationRequest` | read | 4.1 | The med list reads through this because the standard-API list GET 500s on uuid-less rows. |
| `Encounter` | read | 4.1 | Returns every row TWICE on 8.4; the transport dedupes by id. Still present as of the 4.5 preflight. |
| `CarePlan` | read | 4.1 | Read only; the plan of care is authored in the app and filed as a document. |
| `DocumentReference` | read | 4.5 | 403 until the Phase 8.6 org ACL grant; 200 since 2026-09-08. Reported total 0 instance-wide even for filed documents, which is why the chart index is assembled app-side and merges whatever FHIR returns. |
| `Observation` | read | 4.1 | Vital-signs category only. The bundle grew after the 8.4 vitals POST fix, which is how that fix was confirmed. |
| `Practitioner` | read | 4.2 | 403 at the ACL layer until the 8.6 grant. The provider list degrades to mapped app clinicians rather than reading empty. |
| `encounter` | write | 4.1 | Create and update. The PUT needs `user` and `group` in the body or it answers HTTP 200 with a validationErrors map and writes NOTHING — check the body, never the status code. |
| `soap_note` | write | 4.4 | Keyed by NUMERIC pid and eid; uuid-keyed writes coerce to pid 0 and orphan. The list endpoint leaks other encounters' rows, so notes are read back by sid. |
| `vital` | write | 4.5 | The 7.0.4 unconditional 500 is fixed on 8.4; POST returns 201 and the Observation bundle grows. |
| `medical_problem` | write | 4.1 | begdate must be a plain date. |
| `allergy` | write | 4.1 | begdate must be a plain DATE — a datetime is refused as HTTP 200 with data: []. Severity and reaction ride in the comment because severity_ale and reaction are empty option lists on this instance. |
| `medication` | write | 4.1 | Keyed by numeric pid, resolved and cached from the uuid. |
| `prescription` | write | 4.5 | Links on `encounter` with the NUMERIC eid; the uuid silently does not link. Reads date_added — start_date is accepted and discarded. Route and frequency ride in the note because drug_route and drug_interval are empty option lists here. |
| `appointment` | write | 4.2 | No update route on 8.4 — reschedule and cancel use the tombstone swap. The LIST endpoints omit pc_hometext, pc_duration and pc_room; only the single-row GET returns them. Create can answer 200 with a validationErrors map. |
| `facility` | read | 4.5 | Where the POS actually comes from. Hickory Log still has service_location unset. |
| `document` | write | 4.5 | Keyed by NUMERIC pid. The uuid is rejected, which silently stopped care-plan filing after the 8.4 upgrade. |
| `document` | read | 4.9 | Through the 6B patch, singular /document — the plural path would demand a sixth OAuth scope and a new client. Verified 29/29 with bytes read back. DELETE the override block when upstream fixes its CSRF-500 read. |
| `billing` | write | 4.5 | The 6B patch over BillingUtilities::addBilling(). Acceptance 17/17, and it took three runs — assert stored values, not status codes. |
| `order` | write | 4.5 | procedure_order plus order-code rows. The controller reads name/title on the order half and code_text on the charge half, so all three keys are sent. |
| `codes` | read | 4.5 | Through OpenEMR's own main_code_set_search. ICD-10 returns; CPT returns 0 rows and that is CORRECT — AMA copyright, OpenEMR ships none. |

## Wired, not proven (9)

The code exists and is called. **Nobody has run it against a real EMR.** It may 403 on the org-level ACL, 404 on a route 8.4 does not serve, or work perfectly — and those are three different problems with three different fixes. Saying so is the point of the row; an empty result is not a diagnosis.

| Resource | Kind | Since | What to know |
|---|---|---|---|
| `Coverage` | read | 4.12 | Answered 403 on the org ACL before the 8.6 grant and 200 after (verified 2026-09-08) — but nothing has read an actual coverage row back. The payer summary the app bills from is its own, from intake. |
| `Immunization` | read | 4.12 | Never called before this session. Home-based primary care gives flu and COVID vaccines, so this is the one most likely to hold real rows. |
| `CareTeam` | read | 4.12 | The app holds its own careTeam on the client record and that stays the source of truth for routing an escalation. This read is for what OpenEMR believes, which may differ. |
| `RelatedPerson` | read | 4.12 | Family and POA live on the app's user records; this is OpenEMR's own view. Never treat it as the POA designation — that is an app field set only after the document is verified. |
| `Goal` | read | 4.12 | Care-plan goals are authored in the app. This read exists so a goal entered directly in OpenEMR is visible rather than invisible. |
| `Device` | read | 4.12 | DME ordered through 4.10 is app-side and does not write here. A device recorded in OpenEMR would otherwise be unreadable from the app. |
| `Media` | read | 4.12 | Wound photographs are the realistic content. 8.4 may not route it at all; the chart says which failure it got. |
| `QuestionnaireResponse` | read | 4.12 | Screening instruments (PHQ-9, GAD-7) if they are ever entered in OpenEMR. The app does not write them. |
| `Procedure` | read | 4.12 | Orders file into procedure_order through the 6B patch, not through FHIR. Whether they surface here is unknown. |

## Deliberately not wired (8)

A gap somebody **chose**, with the reason. That is a different fact from a gap nobody noticed, and only one of them is a bug.

| Resource | Kind | Since | What to know |
|---|---|---|---|
| `AuditEvent` | read | — | DELIBERATE — it 404s on 8.4, as do api/log and fhir/Provenance. An accounting of disclosures cannot be produced from OpenEMR; the app's own durable audit_log (Session 5.4) is the record. |
| `Coverage` | write | — | DELIBERATE — eligibility is Track D / B1 (Availity). Writing a coverage row the app cannot verify would put an unchecked payer on a claim. |
| `Immunization` | write | — | DELIBERATE — recording an administration needs lot, site, route and a VIS date, which nothing in the app collects. A half-recorded immunization is worse than none. |
| `Questionnaire` | write | — | DELIBERATE — screening instruments are scored, and a score written without the instrument that produced it is a number nobody can check. |
| `Media` | write | — | DELIBERATE — a wound photograph goes to the chart as a document through the route that already exists and is proven, rather than through a second unproven path. |
| `Goal` | write | — | DELIBERATE — care-plan goals are authored and versioned in the app, and the signed plan is filed as a document. Two writers for one goal is how the two disagree. |
| `CareTeam` | write | — | DELIBERATE — the app's careTeam routes escalations and decides visibility. Mirroring it into OpenEMR would create a second answer to who is on this patient's team. |
| `procedure` | write | — | DELIBERATE — user/procedure.write does not exist on 8.4 (verified at the 4.5 preflight). Orders go through the 6B order route instead. |

## How a row moves

From **wired, not proven** to **proven** by running it against the live instance and reading the stored value back, then editing `openemrCoverage.js` and re-running this script. Never by reading the code and deciding it looks right.
